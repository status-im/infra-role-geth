---
- name: Geth | Enable ports on firewall
  iptables:
    comment: '{{ geth_cont_name }}'
    jump: ACCEPT
    action: insert
    chain: DOCKER-USER
    source: '0.0.0.0/0'
    protocol: 'tcp'
    destination_port: '{{ geth_port }}'
  notify:
    - Save iptables rules
